import 'package:flutter/cupertino.dart';

class QueryablePage extends StatelessWidget {
  final Function onRandomButtonClick;

  const QueryablePage({Key key, this.onRandomButtonClick}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
